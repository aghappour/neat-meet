/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The AudioWorklet processor is served as a static asset from /public.
  // Nothing special needed here yet, but this file is the hook for future config.
};

export default nextConfig;
